apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-nginx-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hello-nginx
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: hello-nginx
    spec:
      containers:
        - name: hello-nginx-pod
          image: purplepip/hello:v1.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: MY_SECRET
              valueFrom:
                secretKeyRef:
                  name: mock-secret
                  key: my-secret